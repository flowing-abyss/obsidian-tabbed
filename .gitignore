# vscode
.vscode

# Intellij
*.iml
.idea

# npm
node_modules

# Don't include the compiled main.js file in the repo.
# They should be uploaded to GitHub releases instead.
main.js

# Exclude sourcemaps
*.map

# obsidian
data.json

# Exclude macOS Finder (System Explorer) View States
.DS_Store

# Coverage
coverage/

# E2E: downloaded Obsidian binaries/APKs, per-run WDIO output
.obsidian-cache/
tests/e2e/wdio-logs/
allure-results/

# AI — anchored (leading /) so these only match the generated symlinks at
# the repo root, not the real files nested under .ai/configs/ that share
# the same directory names (e.g. .ai/configs/.claude/, .ai/configs/.codex/).
# An earlier unanchored version of this block silently excluded those real
# files from every commit — see .ai/setup.mjs and the AGENTS.md mirroring
# it describes.
/.a5c/
/.agent/
/.agents/
/.claude/
/.codegraph/
/.codex/
/.forge/
/.opencode/
/.pi-lens/
/.pi/
/.serena/
/.superpowers/
/.worktrees/
/.zed/

/.mcp.json
/AGENTS.md
/CLAUDE.md
/opencode.json

